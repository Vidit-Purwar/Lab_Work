// program to check greater of the two numbers

#include <stdio.h>
int main()
{
    int a,b;
    printf("enter first number ");
    scanf("%d",&a);
    printf("enter second number ");
    scanf("%d",&b);
    if (a>b)
    {printf("larger of the greater number is %d",a);}
    else
    {printf("larger of the greater number is %d",b);}
}
